// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

// Используем SQLite для локальной разработки
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// 🆕 НОВАЯ МОДЕЛЬ: Пользователи
// Здесь будут храниться все зарегистрированные пользователи
model User {
  id       Int    @id @default(autoincrement())  // Уникальный ID (автоматически)
  email    String @unique                         // Email - должен быть уникальным!
  password String                                 // Хешированный пароль (НЕ в открытом виде!)
  
  createdAt DateTime @default(now())              // Когда создан аккаунт
  updatedAt DateTime @updatedAt                   // Когда последний раз обновлялся
  
  // 🔗 СВЯЗЬ: У одного пользователя может быть много контактов
  // "contacts" - это название поля через которое мы получим все контакты юзера
  contacts Contact[]  
  
  @@map("users")  // Имя таблицы в базе данных будет "users"
}

// 📝 ОБНОВЛЕННАЯ МОДЕЛЬ: Контакты
// Теперь каждый контакт принадлежит конкретному пользователю!
model Contact {
  id    Int    @id @default(autoincrement())
  name  String
  phone String
  
  // 🆕 ДОБАВЛЯЕМ: Связь с пользователем
  userId Int    // ID пользователя, которому принадлежит этот контакт
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  // ☝️ Что это значит:
  // - fields: [userId] - наше поле которое хранит ID
  // - references: [id] - ссылается на поле id в модели User
  // - onDelete: Cascade - если удалим юзера, удалятся ВСЕ его контакты автоматически
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("contacts")
}